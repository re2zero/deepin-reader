# NON-AUTOMATABLE: 性能/139 "多平台测试"
# This test case requires running the same application on 兆芯/龙芯/ARM
# hardware platforms to verify functionality. This requires separate physical
# machines or emulators that cannot be provisioned from the current test runner.
# Manual testing required across each target platform.

name: "性能/139 多平台测试 — NON-AUTOMATABLE"
app: "deepin-reader"
screenshot: false

setup:
  - action: session_start
    command: "deepin-reader /home/uos/work/deepin-reader/tests/files/normal.pdf"
    wait: 3.0

steps:
  - name: "无法自动化: requires 兆芯/龙芯/ARM platforms"
    action: wait
    action: wait
    wait: 1.0

teardown:
  - action: session_stop
